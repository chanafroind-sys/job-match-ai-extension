# JMA V2 backend package — isolated from the frozen V1 code in main.py.
# All V2 routes live under /api/v2/*; V2 modules never import flow logic
# from main.py (physical duplication per the V1↔V2 replication contract).
